# Use official lightweight Node.js image
FROM node:18-alpine

# Set working directory
WORKDIR /app

# Install dependencies only from package.json/package-lock.json first (better layer caching)
COPY package*.json .

# Install dependencies 
RUN npm install

# Copy all files from the project root to the container
COPY . .

# Expose Vite default port
EXPOSE 5173

# Optional: Copy env file (can also be done at runtime via `docker run --env-file`)
# COPY .env.local .env                                   # passing through docker-compose

# Command to run the application
CMD ["npm", "run", "dev", "--", "--host"]